{%- comment -%}
  ============================================================================
  Gift Guide — Popup shell  (shared, rendered once by the grid section)
  ----------------------------------------------------------------------------
  A single hidden dialog reused for every product. It contains only the static
  structure (image, title, price, description, color swatches, size select,
  add-to-cart). JavaScript injects the selected product's content on open.

  Accessibility: role="dialog", aria-modal, focus trap, Esc / overlay to close.

  PHASE STATUS
    [x] Phase 1  scaffold (this file, current)
    [ ] Phase 5  dialog markup + a11y hooks
  ============================================================================
{%- endcomment -%}

{%- comment -%} TODO Phase 5: popup dialog markup. {%- endcomment -%}
